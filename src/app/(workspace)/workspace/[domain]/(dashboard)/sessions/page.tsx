import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sessions",
  description: "Manage your active sessions",
};

export default function WorkspaceSessionsPage() {
  return <div>Workspace Sessions</div>;
}
